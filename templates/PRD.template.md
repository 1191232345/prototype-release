# PRD - {项目名称}

> 评审对照文档：对照原型时以 **第 3 章「页面交互规格」** 为准。
> 填写说明：将 `{占位符}` 替换为实际内容；无此项的表格行可删除。
>
> **产品语言约定**（第 3 章必读）：
> - **只写业务语言**：用中文描述检索项、列名、字段名、按钮文案、状态名、操作结果。
> - **禁止写入**：字段 ID、column key、表名、库表字段、API 路径、mock 变量、`navigate()`、action key 等实现细节。
> - **数据来源**：一律写 **功能菜单路径**，格式 `一级菜单 > 二级菜单 > 页面/弹窗`；本页已有数据写「本页列表」；计算/联动写「本页计算」或「由…联动带出」。
> - **技术对照**：Spec JSON 中的 id/key 由原型与开发维护；PRD 仅通过 `{#锚点}` 与原型联动，不重复记录技术标识。

---

## 1. Executive Summary

- **Problem Statement**：{业务问题一句话}
- **Proposed Solution**：{解决方案一句话}
- **Success Criteria**：
  - {可量化成功标准 1}
  - {可量化成功标准 2}

---

## 2. User Experience & Functionality

### User Personas

- **{角色 A}**：{职责与使用场景}
- **{角色 B}**：{职责与使用场景}

### User Stories

- **Story 1**：作为{角色}，我希望{能力}，以便{价值}。
- **Story 2**：作为{角色}，我希望{能力}，以便{价值}。

### Acceptance Criteria

- [ ] {可验收的高层标准 1}
- [ ] {可验收的高层标准 2}

### Non-Goals

- {明确不做的事项 1}
- {明确不做的事项 2}

---

## 3. 页面交互规格

> 本章按 **初始化 → 检索 → 列表属性 → 主要按钮 → 列表行按钮** 组织。
> 多页面项目请复制下列 `## 3.x` 小节，按 list / form / 弹窗分别编写。
> **每个小节使用独立 `##` 标题**，以便对照评审工具渲染为卡片。
> **标题末尾须加锚点** `{#锚点ID}`（如 `{#list.filters}`），与原型 `data-review-id` 对照定位。

## 3.1 列表页 · 初始化与数据展示逻辑 {#list.init}

| 逻辑项 | 说明 | 数据来源 | 展示规则 | 备注 |
|--------|------|----------|----------|------|
| 页面加载 | {进入列表页时执行的初始化动作} | 本页列表 | {加载哪些数据、初始化哪些控件} | {如：首次无数据时的表现} |
| 默认排序 | {列表默认排序规则} | 本页列表 | {如：创建时间倒序，最新在上} | |
| 空状态处理 | {无数据时的展示} | 本页列表 | {空状态文案、引导按钮} | |
| {其他逻辑项} | | | | |

## 3.1 列表页 · 检索条件说明 {#list.filters}

| 检索项 | 输入方式 | 匹配逻辑 | 触发时机 | 数据来源 | 备注 |
|--------|----------|----------|----------|----------|------|
| {标签，如：客户} | {下拉 / 文本 / 日期} | {精确 / 模糊 / 范围} | {实时 / 点击查询} | {客户管理 > 客户列表} | {默认值、是否多选} |
| {标签，如：仓库} | | | | {仓库管理 > 仓库列表（启用状态）} | |
| 查询按钮 | 点击 | 按当前条件过滤列表 | 点击时 | 本页列表 | {与实时筛选的关系} |
| 重置按钮 | 点击 | 清空所有检索条件并恢复全量 | 点击时 | — | |

## 3.1 列表页 · 列表属性说明 · 列字段取值 {#list.columns}

| 列名 | 取值规则 | 数据来源 | 备注 |
|------|----------|----------|------|
| {列标题} | {如何取值、格式化} | {本页列表 / 关联菜单 > 页面} | {如：主副行、截断规则} |
| {列标题} | {本页计算} | 本页计算 | |

## 3.1 列表页 · 列表属性说明 · 展示样式 {#list.column-styles}

| 列名 | 样式规则 | 特殊效果 |
|------|----------|----------|
| {列标题} | {对齐、字号、加粗等} | {悬停、标签色、过期样式等} |
| 状态 | 标签居中 | {草稿→灰 / 已发布→绿 / 已作废→红} |
| 操作列 | 右对齐 | {按行状态动态显示按钮，见「列表按钮说明」} |

## 3.1 列表页 · 主要按钮说明 {#list.main-buttons}

> 页面级 / 工具栏按钮（非表格行内操作）。

| 按钮 | 位置 | 触发动作 | 前置条件 | 后续操作 |
|------|------|----------|----------|----------|
| 新增 | 筛选区下方左侧 | 进入新建表单页 | 无 | {表单初始状态说明} |
| 查询 | 筛选区右侧 | 执行筛选并刷新表格 | 无 | {筛选逻辑引用「检索条件说明」} |
| 重置 | 查询按钮旁 | 清空检索条件 | 无 | 恢复全量列表 |
| {导出/导入等} | | | | |

## 3.1 列表页 · 列表按钮说明 {#list.row-buttons}

> 表格行内操作；「显示条件」指何种行状态/业务条件下展示该按钮。

| 按钮 | 显示条件 | 触发动作 | 前置条件 | 后续操作 |
|------|----------|----------|----------|----------|
| 查看 | {如：所有状态} | 打开详情弹窗 | 行数据存在 | {弹窗展示内容摘要} |
| 编辑 | {如：草稿} | 进入编辑表单页，带入当前行数据 | {状态 = 草稿} | {表单预填充规则} |
| 发布 | {如：草稿} | 确认后变更为已发布 | {状态 = 草稿} | {校验规则、成功提示} |
| 删除 | {如：草稿} | 确认后删除 | {状态 = 草稿} | {二次确认、刷新列表} |
| 作废 | {如：已发布} | 确认后变更为已作废 | {状态 = 已发布} | |
| 复制 | {如：已作废} | 复制数据创建新草稿 | | {跳转表单、命名规则} |

## 3.1 列表页 · 状态变更流程 {#list.state-flow}

| 当前状态 | 允许操作 | 目标状态 | 说明 |
|----------|----------|----------|------|
| — | 新建 / 暂存 | 草稿 | |
| 草稿 | 发布 | 已发布 | {是否可跳过草稿直接发布} |
| 已发布 | 作废 | 已作废 | 不可逆 |
| 已作废 | 复制新增 | 新草稿 | {副本命名规则} |

## 3.1 列表页 · 详情弹窗说明 {#list.detail-modal}

| 区块 | 展示内容 | 只读/可编辑 | 备注 |
|------|----------|-------------|------|
| {基本信息} | {字段列表} | 只读 | |
| {业务明细} | | | |

---

## 3.2 表单页 · 初始化与数据展示逻辑 {#form.init}

| 逻辑项 | 说明 | 数据来源 | 展示规则 | 备注 |
|--------|------|----------|----------|------|
| 页面加载 | {初始化下拉选项、清空字段} | {各下拉对应菜单，见「表单字段说明」} | | |
| 模式识别 | {新建 vs 编辑} | 本页 | 从列表进入且带选中行 → 编辑；否则 → 新建 | |
| 编辑预填充 | {编辑模式下字段如何赋值} | {列表页 > 选中行} | {哪些字段预填、哪些禁用} | |
| 新建默认值 | {新建模式下默认值} | | {如：生效时间默认当前} | |

## 3.2 表单页 · 表单字段说明 {#form.fields}

| 字段 | 类型 | 必填 | 新建模式 | 编辑模式 | 数据来源 | 备注 |
|------|------|------|----------|----------|----------|------|
| {标签} | {文本 / 下拉 / 日期 / …} | 是/否 | {空 / 可选 / 默认值} | {预填充 / 禁用 / 只读} | {客户管理 > 客户列表} | |
| {标签} | | | | | {价卡管理 > 价卡列表} | |

## 3.2 表单页 · 字段验证规则 {#form.validation}

| 字段 | 验证规则 | 触发时机 | 错误提示 |
|------|----------|----------|----------|
| {字段} | {必填 / 格式 / 范围 / 唯一性} | {失焦 / 提交} | {提示文案} |
| {跨字段校验} | | | |

## 3.2 表单页 · 表单交互逻辑 {#form.interaction}

| 触发条件 | 交互逻辑 | 展示变化 |
|----------|----------|----------|
| {如：选择客户+仓库} | {联动逻辑} | {名称自动生成、重复提示等} |
| {如：选择某模板} | | |

## 3.2 表单页 · 主要按钮说明 {#form.main-buttons}

| 按钮 | 位置 | 触发动作 | 前置条件 | 后续操作 |
|------|------|----------|----------|----------|
| 取消 | 表单底部 | 返回列表页，不保存 | 无 | — |
| 暂存 | 表单底部 | 保存为草稿 | {校验规则} | {提示 + 返回列表} |
| 发布/保存 | 表单底部 | 校验通过后提交 | {全部必填通过} | {提示 + 返回列表} |

---

## 3.3 {其他页面} · {小节标题}

（按 3.1 / 3.2 相同结构补充，或删除本节）

---

## 4. 实现约束（可选）

> 本节供排期与跨团队对齐，**不写库表、接口字段、技术标识**。

- **关联功能**：{需读取或写入的业务菜单，如：客户管理 > 客户列表、价卡管理 > 价卡详情}
- **权限说明**：{谁可以查看 / 新建 / 编辑 / 发布 / 作废}
- **性能预期**：{如列表筛选响应时间、并发量等}

---

## 5. Risks & Roadmap

### Phased Rollout

- **MVP**：{首版范围}
- **v1.1**：{下一迭代}
- **v2.0**：{远期}

### Technical Risks

- {风险 1}
- {风险 2}
